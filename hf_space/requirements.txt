# VoiceTut-TTS Space dependencies.
torch
# OmniVoice backbone (must be installed explicitly — voicetut-tts no longer auto-pulls it)
omnivoice @ git+https://github.com/k2-fsa/OmniVoice.git
# this package (from GitHub so the Space always has the latest app + speakers)
voicetut-tts @ git+https://github.com/MohammedAly22/VoiceTuT-TTS.git
gradio>=4.0
